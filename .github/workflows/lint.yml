name: Lint

on:
  pull_request_target:
    branches:
      - main
      - release-v*

jobs:
  markdownlint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: DavidAnson/markdownlint-cli2-action@v6
      with:
        globs: '**/*.md'
