name: Changelog Enforcer

on:
  pull_request_target:
    branches:
      - main
      - release-*
  push:
    branches:
      - main
      - release-*

permissions:
  contents: read

jobs:
  changelog-enforcer:
    runs-on: ubuntu-latest
    steps:
      - uses: dangoslen/changelog-enforcer@v3
